{
  "name": "grokgrok",
  "version": "0.1.0",
  "private": true,
  "description": "Clean-room reconstruction and architecture atlas for Grok Bot 0.24+",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "ingest": "node scripts/ingest.mjs",
    "inventory": "node scripts/ingest.mjs --inventory-only",
    "validate": "node scripts/validate-all.mjs",
    "gen:desktop-bridge": "node scripts/generate-desktop-bridge.mjs",
    "gen:coordinator-bridge": "node scripts/generate-coordinator-bridge.mjs",
    "test": "node --test tests/coordinator.test.mjs tests/main-rpc.test.mjs",
    "test:all": "npm run test && npm run test:dom",
    "typecheck": "tsc --noEmit",
    "build:desktop": "tsc apps/desktop/app.ts --target es2022 --module esnext --moduleResolution bundler --lib es2022,dom --outDir apps/desktop/dist && cp apps/desktop/index.html apps/desktop/styles.css apps/desktop/dist/",
    "test:dom": "node --test tests/dom.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "jsdom": "^30.0.1",
    "typescript": "^5.9.3"
  }
}
